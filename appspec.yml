version: 1
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "REPLACED_AT_DEPLOYMENT"
        LoadBalancerInfo:
          ContainerName: "WildRydesApp"
          ContainerPort: 8080
